import { createApp } from './app';

export const viteNodeApp = createApp();

// createApp()
//   .then(async (app) => {
//     await app.listen(30000);
//   })
//   .catch((err) => {
//     console.error(err);
//   });
